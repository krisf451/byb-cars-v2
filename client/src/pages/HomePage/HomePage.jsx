import { Home } from '../../components';

const HomePage = () => (
  <div className="flex flex-col items-center justify-center h-auto bg-white dark:bg-darkBg transition-all duration-300 ease-in-out">
    <Home />
  </div>
);

export default HomePage;
